export type DepotStock = {
  symbol: string;
  company: string;
  // floatingStockNumber: string;
  // lastTradeTime: string;
  // lastTradePrice: string;
  // nameStockExchange: string;
  // marketCapitalization: string;
  priceBought: string;
  currentAmount: string;
};
